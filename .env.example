# =====================================================
# VBE Hub - Variaveis de Ambiente
# Copie este arquivo para .env e preencha os valores
# NUNCA faca commit do .env com valores reais!
# =====================================================

# --- Banco de Dados ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=sua_senha_aqui
POSTGRES_DB=vigimanaus

# --- JWT (Seguranca) ---
# Gere uma chave aleatoria de pelo menos 256 bits.
# Exemplo Linux: openssl rand -hex 32
JWT_SECRET=sua_chave_jwt_secreta_aqui_minimo_256_bits
JWT_EXPIRATION=86400000

# --- CORS ---
# Origens permitidas, separadas por virgula
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80

# --- Frontend ---
VITE_API_URL=http://localhost:8081/api
